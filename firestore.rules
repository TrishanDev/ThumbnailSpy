rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // User profile and usage limit rules
    match /users/{userId} {
      // Users can read and update their own document
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }

  }
}
